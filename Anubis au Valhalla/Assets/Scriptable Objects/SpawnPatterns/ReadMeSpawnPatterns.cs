using UnityEngine;

public class ReadMeSpawnPatterns : MonoBehaviour
{
    /* Les Spawns patterns sont appelés selon la nomenclature suivantes : 
     * Patt
     * +
     * E[x] ([x] étant un int) qui indique le nombre d'ennemi différent
     * + 
     * S[x] = ([x] étant un int) indique le nombre d'élite
     * V = contient la Valkyrie
     * VS = contient la Valkyrie Elite
     * Full ... = ne contient que le type d'ennemi précisé
     * +
     * numéro de patterns (suivant le nombre de pattern déjà crées avant */
     
     /* EXEMPLE : Patt E3 VS 2 = groupe de 3 ennemis , avec une Valkyrie élite , 2ème de cette série
     */
}
